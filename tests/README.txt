There should be a working installation of cargo and just (via cargo install just) apart from the installation from the cargo package in R.

Use graphical "Git Bash" under windows.

Test using, e.g., just all > all-mac_m1.txt 2>&1

